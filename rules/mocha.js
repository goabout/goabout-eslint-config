// Rules specific to mocha test framework
module.exports = {
  globals: {
    describe: false,
    it: false,
    beforeEach: false,
    afterEach: false,
    expect: false,
    assert: false,
    sandbox: false,
    request: false
  }
}
